const selectedConnections=new Set();document.querySelectorAll('.tab-btn').forEach(btn=>{btn.addEventListener('click',()=>{document.querySelectorAll('.tab-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.tab-content').forEach(c=>c.style.display='none');document.getElementById('tab-'+btn.dataset.tab).style.display='block'})});document.querySelectorAll('.connection-tag').forEach(tag=>{tag.addEventListener('click',()=>{const id=tag.dataset.id;if(selectedConnections.has(id)){selectedConnections.delete(id);tag.classList.remove('active')}else{selectedConnections.add(id);tag.classList.add('active')}})});document.getElementById('btn-scan').addEventListener('click',()=>{const country=document.getElementById('scan-country').value;if(!country){alert('请选择目标国家');return}const report=window.LawFlowSanctionEnhanced.generateSanctionReport(country,document.getElementById('scan-product').value,document.getElementById('scan-entity').value,Array.from(selectedConnections));displayScanResult(report);prepareLawShield(report)});function displayScanResult(report){document.getElementById('scan-placeholder').style.display='none';document.getElementById('scan-result').style.display='block';document.getElementById('scan-time').textContent='扫描时间: '+new Date().toLocaleString();const scoreEl=document.getElementById('risk-score'),labelEl=document.getElementById('risk-label');scoreEl.textContent=report.overallRisk.score;scoreEl.className='risk-score risk-'+report.overallRisk.level;labelEl.textContent=report.overallRisk.label;labelEl.className='text-lg font-semibold mt-2 risk-'+report.overallRisk.level;document.getElementById('risk-factors').innerHTML=report.riskFactors.map(f=>`<div class="flex justify-between items-center"><span class="text-sm text-gray-600">${f.factor}</span><div class="flex items-center gap-2"><div class="w-24 h-2 bg-gray-200 rounded-full overflow-hidden"><div class="h-full bg-${f.level==='critical'?'red-600':f.level==='high'?'orange-500':f.level==='medium'?'yellow-500':'green-500'}" style="width:${f.score}%"></div></div><span class="text-sm font-medium w-8 text-right">${f.score}</span></div></div>`).join('');const detailsEl=document.getElementById('scan-details');let detailsHTML='';if(report.countryScan){const cs=report.countryScan;detailsHTML+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h4 class="font-semibold text-gray-900 mb-3">国家风险: ${cs.countryRisk?.countryName||''}</h4><div class="space-y-2">${cs.countryRisk?.reasons?.map(r=>`<div class="text-sm text-gray-600 flex items-start gap-2"><span class="text-red-500 mt-0.5">⚠</span>${r}</div>`).join('')||''}</div>${cs.recommendations?.length?`<div class="mt-4 p-4 bg-blue-50 rounded-lg"><div class="text-sm font-medium text-blue-900 mb-2">建议</div>${cs.recommendations.map(r=>`<div class="text-sm text-blue-800">${r}</div>`).join('')}</div>`:''}</div>`;}if(report.entitySearch?.length>0){detailsHTML+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h4 class="font-semibold text-gray-900 mb-3">实体匹配结果 (${report.entitySearch.length})</h4>${report.entitySearch.map(e=>`<div class="entity-card ${e.riskLevel}"><div class="flex justify-between items-start"><div><div class="font-medium text-gray-900">${e.name}</div><div class="text-sm text-gray-500">ID: ${e.id} | 名单: ${e.list} | 国家: ${e.country}</div><div class="text-sm text-gray-600 mt-1">${e.reason}</div><div class="text-xs text-gray-400 mt-1">法律依据: ${e.legalBasis}</div></div><span class="px-3 py-1 rounded-full text-xs font-semibold ${e.riskLevel==='critical'?'bg-red-100 text-red-700':e.riskLevel==='high'?'bg-orange-100 text-orange-700':'bg-yellow-100 text-yellow-700'}">${e.riskLevel==='critical'?'极高':e.riskLevel==='high'?'高':'中'}风险</span></div></div>`).join('')}</div>`;}if(report.countryScan?.secondaryRisk?.connectionAnalysis){const ca=report.countryScan.secondaryRisk.connectionAnalysis;detailsHTML+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h4 class="font-semibold text-gray-900 mb-3">连接点风险分析</h4><div class="text-sm text-gray-600 mb-3">${ca.summary}</div><div class="grid grid-cols-1 md:grid-cols-2 gap-4">${ca.dimensions?.map(d=>`<div class="p-3 bg-gray-50 rounded-lg"><div class="flex justify-between items-center mb-2"><span class="text-sm font-medium">${d.dimension}</span><span class="text-sm font-bold ${d.score>=60?'text-red-600':d.score>=40?'text-orange-500':'text-green-600'}">${d.score}分</span></div><div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden"><div class="h-full ${d.score>=60?'bg-red-500':d.score>=40?'bg-orange-400':'bg-green-500'}" style="width:${d.score}%"></div></div><div class="text-xs text-gray-500 mt-1">权重 ${(d.weight*100).toFixed(0)}% | 触发 ${d.triggeredCount} 个指标</div></div>`).join('')||''}</div></div>`;}if(report.exportControl){const ec=report.exportControl;detailsHTML+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h4 class="font-semibold text-gray-900 mb-3">出口管制分析</h4><div class="text-sm ${ec.licenseRequired?'text-red-600 font-medium':'text-green-600'}">${ec.reason}</div>${ec.eccnList?.length?`<div class="mt-3 space-y-2"><div class="text-sm font-medium text-gray-700">相关ECCN分类:</div>${ec.eccnList.map(e=>`<div class="flex justify-between items-center p-2 bg-gray-50 rounded text-sm"><span><code class="bg-gray-200 px-1 rounded">${e.code}</code> ${e.desc}</span><span class="text-xs ${e.riskLevel==='critical'?'text-red-600':e.riskLevel==='high'?'text-orange-600':'text-green-600'}">${e.riskLevel==='critical'?'极高':e.riskLevel==='high'?'高':'中'}风险</span></div>`).join('')}</div>`:''}</div>`;}detailsEl.innerHTML=detailsHTML;}function prepareLawShield(report){const shield=window.LawFlowLawShieldEnhanced.generateLawShield(report,{country:'CN'});const container=document.getElementById('shield-content');let html=`<div class="space-y-6"><div class="grid grid-cols-1 md:grid-cols-3 gap-4"><div class="bg-white rounded-xl p-4 border border-gray-200 text-center"><div class="text-sm text-gray-500">综合风险</div><div class="text-2xl font-bold ${shield.riskSummary.overallScore>=60?'text-red-600':'text-orange-500'}">${shield.riskSummary.overallScore}分</div></div><div class="bg-white rounded-xl p-4 border border-gray-200 text-center"><div class="text-sm text-gray-500">匹配工具</div><div class="text-2xl font-bold text-indigo-600">${shield.applicableTools}个</div></div><div class="bg-white rounded-xl p-4 border border-gray-200 text-center"><div class="text-sm text-gray-500">合规检查项</div><div class="text-2xl font-bold text-green-600">${shield.complianceChecklist.length}项</div></div></div><div class="bg-white rounded-xl p-6 border border-gray-200"><h3 class="section-title">整体建议</h3><div class="space-y-3">${shield.overallRecommendations.map(r=>`<div class="p-4 rounded-lg ${r.priority==='critical'?'bg-red-50 border border-red-200':r.priority==='high'?'bg-orange-50 border border-orange-200':'bg-blue-50 border border-blue-200'}"><div class="flex items-center gap-2 mb-1"><span class="px-2 py-0.5 rounded text-xs font-semibold ${r.priority==='critical'?'bg-red-200 text-red-800':r.priority==='high'?'bg-orange-200 text-orange-800':'bg-blue-200 text-blue-800'}">${r.priority==='critical'?'紧急':r.priority==='high'?'高优':'建议'}</span><span class="font-semibold text-gray-900">${r.title}</span></div><div class="text-sm text-gray-600 mb-2">${r.desc}</div><div class="text-sm font-medium text-indigo-700">行动: ${r.action}</div></div>`).join('')}</div></div>`;html+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h3 class="section-title">行动方案</h3>${shield.timeline.immediate.length?`<div class="timeline-section"><div class="timeline-title"><span class="timeline-dot immediate"></span>立即行动 (7-14天)</div><div class="space-y-3">${shield.timeline.immediate.map(a=>renderToolCard(a)).join('')}</div></div>`:''}${shield.timeline.shortTerm.length?`<div class="timeline-section"><div class="timeline-title"><span class="timeline-dot short"></span>短期行动 (30-60天)</div><div class="space-y-3">${shield.timeline.shortTerm.map(a=>renderToolCard(a)).join('')}</div></div>`:''}${shield.timeline.longTerm.length?`<div class="timeline-section"><div class="timeline-title"><span class="timeline-dot long"></span>长期行动 (90天+)</div><div class="space-y-3">${shield.timeline.longTerm.map(a=>renderToolCard(a)).join('')}</div></div>`:''}</div>`;html+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h3 class="section-title">合规检查清单</h3><div class="grid grid-cols-1 md:grid-cols-2 gap-4">${shield.complianceChecklist.map(item=>`<div class="checklist-item"><div class="check-box"></div><div><div class="text-sm font-medium text-gray-900">${item.item}</div><div class="text-xs text-gray-500">${item.category} · ${item.frequency} · ${item.responsible}</div></div></div>`).join('')}</div></div>`;html+=`<div class="bg-white rounded-xl p-6 border border-gray-200"><h3 class="section-title">可用文书模板</h3><div class="grid grid-cols-2 md:grid-cols-4 gap-4">${shield.documentTemplates.map(t=>`<div class="template-card"><div class="text-sm font-medium text-gray-900">${t}</div><div class="text-xs text-gray-500 mt-1">点击查看</div></div>`).join('')}</div></div></div>`;container.innerHTML=html;container.classList.remove('placeholder-box');}function renderToolCard(action){return `<div class="tool-card"><div class="tool-header"><div><div class="tool-category">${action.category}</div><div class="font-semibold text-gray-900 mt-1">${action.toolName}</div></div><span class="success-rate">成功率: ${action.successRate}</span></div><div class="text-sm text-gray-600 mb-3"><span class="font-medium">法律依据:</span> ${action.legalBasis}</div><div class="text-sm text-gray-600 mb-3"><span class="font-medium">预计时间:</span> ${action.timeFrame}</div><div class="space-y-2 mb-3">${action.steps.map(s=>`<div class="flex items-start gap-3 text-sm"><span class="w-6 h-6 rounded-full bg-indigo-100 text-indigo-700 flex items-center justify-center text-xs font-bold flex-shrink-0">${s.step}</span><span class="text-gray-700">${s.description}</span></div>`).join('')}</div><div class="p-3 bg-yellow-50 rounded-lg text-sm text-yellow-800"><span class="font-medium">⚠ 风险提示:</span> ${action.riskWarning}</div>${action.templates?.length?`<div class="mt-3 flex flex-wrap gap-2">${action.templates.map(t=>`<span class="text-xs px-2 py-1 bg-gray-100 rounded text-gray-600">${t.name}</span>`).join('')}</div>`:''}</div>`;}document.getElementById('btn-contract').addEventListener('click',()=>{const text=document.getElementById('contract-text').value;if(!text.trim()){alert('请输入合同文本');return}const risks=[];const checks=[{pattern:/制裁|sanction|OFAC|SDN/i,name:'制裁条款缺失',risk:'high',suggestion:'建议加入制裁合规条款，明确双方不在制裁清单上的声明'},{pattern:/不可抗力|force majeure/i,name:'不可抗力条款',risk:'medium',suggestion:'建议明确将「制裁」纳入不可抗力定义范围'},{pattern:/争议解决|arbitration|dispute resolution/i,name:'争议解决条款',risk:'medium',suggestion:'建议优先选择仲裁（SIAC/HKIAC/CIETAC），避免美国法院管辖'},{pattern:/美元|\$|USD/i,name:'美元结算风险',risk:'high',suggestion:'如交易涉及制裁风险国家，建议改用人民币或其他货币结算'},{pattern:/美国|USA|U\.S\./i,name:'美国法律连接点',risk:'medium',suggestion:'评估美国法律适用是否带来制裁合规风险'},{pattern:/出口管制|export control|EAR|ECCN/i,name:'出口管制条款',risk:'high',suggestion:'建议加入出口管制合规条款，明确ECCN分类义务'}];checks.forEach(c=>{if(c.pattern.test(text))risks.push(c)});const resultEl=document.getElementById('contract-result');resultEl.classList.remove('placeholder-box');resultEl.innerHTML=`<div class="bg-white rounded-xl p-6 border border-gray-200 h-full"><h3 class="section-title">审查结果</h3><div class="mb-4"><div class="text-sm text-gray-500">扫描关键词</div><div class="text-2xl font-bold ${risks.length>3?'text-red-600':risks.length>0?'text-orange-500':'text-green-600'}">${risks.length} 项</div></div><div class="space-y-3">${risks.length===0?'<div class="text-green-600">✓ 未检测到明显高风险条款</div>':''}${risks.map(r=>`<div class="p-4 ${r.risk==='high'?'bg-red-50 border border-red-200':'bg-orange-50 border border-orange-200'} rounded-lg"><div class="flex items-center gap-2 mb-1"><span class="px-2 py-0.5 rounded text-xs font-semibold ${r.risk==='high'?'bg-red-200 text-red-800':'bg-orange-200 text-orange-800'}">${r.risk==='high'?'高风险':'中风险'}</span><span class="font-semibold text-gray-900">${r.name}</span></div><div class="text-sm text-gray-700">${r.suggestion}</div></div>`).join('')}</div><div class="mt-6 p-4 bg-blue-50 rounded-lg"><div class="text-sm font-medium text-blue-900">💡 建议</div><div class="text-sm text-blue-800 mt-1">本审查为AI初步扫描，建议由专业律师进行完整合同审查。重点关注：制裁合规条款、不可抗力、争议解决、法律适用等条款。</div></div></div>`;});